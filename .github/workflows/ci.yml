name: CI

# Automatic, offline proof checks. No model API key is available to this job.

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  offline-proof:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: "3.13"
          cache: pip
      - run: python -m pip install ".[dev]"
      - run: python -m pytest
      - run: python -m ruff check .
      - run: python -m contextproof.cli --json --require-ready > /tmp/context-proof.json
